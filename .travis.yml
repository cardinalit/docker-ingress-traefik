---
services:
  - docker

before_script:
  - export STACK_NAME=$(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 9 | head -n 1)
  - docker swarm init

script:
  - ./ingress-up.sh test@example.com
  - ./wait-for-it.sh localhost:8080 --timeout=360 --strict -- curl -v localhost:8080/ping
